# No API key needed — the app uses Claude Code's built-in authentication.
# The server spawns `claude --print` which picks up your existing Claude Code login.

# Optional overrides:
PORT=3001
# CLAUDE_BIN=/path/to/claude   # only needed if `claude` isn't in your PATH
